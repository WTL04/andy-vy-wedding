# Copy to .env and fill in your values. (.env is gitignored — never commit real URLs.)
# Google Apps Script Web App URL for RSVP submissions.
# Deploy your script as a Web app (Execute as: Me, Who has access: Anyone)
# and paste the /exec URL here.
VITE_GOOGLE_SHEETS_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
